# PostgreSQL (Docker compose mặc định port 5433)
DATABASE_URL="postgresql://fohoway:fohoway@localhost:5433/fohoway?schema=public"
# AI Chatbot
ANTHROPIC_API_KEY=""

# Session signing secret (đổi trong production)
AUTH_SECRET="change-me-in-production"
# Neon: direct connection (cho prisma migrate/seed). Local đặt = DATABASE_URL.
DIRECT_URL="postgresql://user:pass@ep-xxx.ap-southeast-1.aws.neon.tech/fohoway?sslmode=require"
